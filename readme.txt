#The examples in this repo are built as part of a hands-on AI bootcamp, where I’ve been actively learning and applying cutting-edge agentic AI concepts.
python -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt
pip --version
autogenstudio ui --port 8081 --appdir ./dir
